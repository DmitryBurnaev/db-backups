version: '3'
services:
  do:
    image:
      db-backups
    build:
      context: .
    env_file:
      - .env
    volumes:
      - ${LOCAL_PATH}:/db-backups/backups
      - $(pwd)/logs:/db-backups/backups:rw
    network_mode: "host"
